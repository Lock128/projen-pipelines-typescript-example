# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release-prod
on:
  workflow_dispatch:
    inputs:
      version:
        description: Package version
        required: true
jobs:
  deploy:
    name: Release stage prod to AWS
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@master
        with:
          role-session-name: GitHubAction
          aws-region: eu-west-1
      - run: npx projen install:ci
      - run: yarn add @projen-pipelines-test/projen-pipelines-typescript-example@${{github.event.inputs.version}} && mv ./node_modules/@projen-pipelines-test/projen-pipelines-typescript-example cdk.out
      - run: npx projen deploy:prod
