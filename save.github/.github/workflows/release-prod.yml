# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release-prod
on:
  workflow_dispatch:
    inputs:
      version:
        description: Package version
        required: true
jobs:
  deploy:
    name: Release stage prod to AWS
    needs: []
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: eu-west-1
      - run: npx projen install:ci
      - run: npm install @projen-pipelines-test/projen-pipelines-typescript-example@${{github.event.inputs.version}}
      - run: mv ./node_modules/@projen-pipelines-test/projen-pipelines-typescript-example cdk.out
      - run: npx projen deploy:prod
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cdk-outputs-prod
          path: cdk-outputs-prod.json
