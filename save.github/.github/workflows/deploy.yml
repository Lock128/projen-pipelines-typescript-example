# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: deploy
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  synth:
    name: Synth CDK application
    needs: []
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: npx projen install:ci
      - run: npx projen build
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
  assetUpload:
    name: Publish assets to AWS
    needs: synth
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: git config --global user.name "github-actions" && git config --global user.email "github-actions@github.com"
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: us-east-1
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: npx projen install:ci
      - run: npx projen publish:assets
      - run: npx projen bump
      - run: npx projen release:push-assembly
  deploy-dev:
    name: Deploy stage dev to AWS
    needs: assetUpload
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: npx projen install:ci
      - run: npx projen deploy:dev
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cdk-outputs-dev
          path: cdk-outputs-dev.json
  deploy-dev2:
    name: Deploy stage dev2 to AWS
    needs:
      - assetUpload
      - deploy-dev
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: npx projen install:ci
      - run: npx projen deploy:dev2
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cdk-outputs-dev2
          path: cdk-outputs-dev2.json
  deploy-staging:
    name: Deploy stage staging to AWS
    needs:
      - assetUpload
      - deploy-dev2
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: npx projen install:ci
      - run: npx projen deploy:staging
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cdk-outputs-staging
          path: cdk-outputs-staging.json
  deploy-preprod:
    name: Deploy stage preprod to AWS
    needs:
      - assetUpload
      - deploy-staging
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: npx projen install:ci
      - run: npx projen deploy:preprod
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cdk-outputs-preprod
          path: cdk-outputs-preprod.json
